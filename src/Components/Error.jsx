import img from '../assets/error.jpg'

const Error = () => {
    return (
        <div style={{ backgroundImage: `url(${img})` }} className='h-screen w-full bg-contain bg-center bg-no-repeat'>
            
        </div>
    );
};

export default Error;